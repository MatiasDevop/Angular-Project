//mysql only use callbacks so use promise-mysql to use promise
// cors is to use interconextion with restapi
// commands to make project
npm init --yes
npm i express morgan promise-mysql cors
npm install -g typescript 
tsc --init
change to es6 version typscript currently
outDir...
after that use tsc again to create build file
change package json build....after that npm run build
install npm i nodemon -D to development
npm i @types/express -D
the same for any module what you want.
npm i @types/morgan @types/cors -D